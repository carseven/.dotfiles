set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

# Reload config file
bind r source-file $HOME/.config/tmux/tmux.conf \; display-message "Config reloaded..."

# Change the default prefix to C-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Clear screen
bind -n C-M-l send-keys clear

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Set status bar top
set-option -g status-position top

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# Documentation
# https://github.com/dreamsofcode-io/catppuccin-tmux/blob/main/catppuccin.tmux
set -g @catppuccin_flavour 'macchiato'
set -g @catppuccin_host "off"
set -g @catppuccin_user "off"
set -g @catppuccin_date_time "off"

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible' # Remove, just add the options i like https://github.com/tmux-plugins/tmux-sensible
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
set -g @plugin 'tmux-plugins/tmux-yank' # Copy to the system clipboard
set -g @plugin 'Nybkox/tmux-lazygit' # Prefix + g (toggle lazygit window in the session)

run '~/.tmux/plugins/tpm/tpm'

# set vi-mode
set-window-option -g mode-keys vi

# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# Remove default confirmation
bind-key & kill-window
bind-key x kill-pane

