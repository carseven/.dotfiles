- name: Set .yabairc executable
  shell: chmod +x ~/.yabairc
  ignore_errors: yes
  tags:
    - yabai
    - skhd

- name: Start yabai brew service
  shell: brew service start yabai
  ignore_errors: yes
  tags:
    - yabai
    - skhd

- name: Start skhd brew service
  shell: brew service start yabai
  ignore_errors: yes
  tags:
    - yabai
    - skhd
